/**
 * API CONFIGURATION
 * This file provides the base URL for all API calls.
 * In development, Vite's proxy handles the request.
 * In production, the backend serves the frontend, so relative URLs work directly.
 */

// Always use a relative URL. The Vite dev server will proxy /api requests.
export const API_BASE_URL = '';
